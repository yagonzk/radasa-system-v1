import { useEffect, useState } from "react";
import { ArrowLeft, ScrollText } from "lucide-react";
import { Link } from "wouter";
import { api } from "@/lib/api";
import Layout from "@/components/Layout";

type Log = { id:string; action:string; createdAt:string; user:{username:string;email:string} };
export default function Logs(){ const [logs,setLogs]=useState<Log[]>([]); const [loading,setLoading]=useState(true); useEffect(()=>{api.get<Log[]>("/logs").then(r=>setLogs(r.data)).finally(()=>setLoading(false))},[]); return <Layout><div><Link href="/" className="mb-5 inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="h-4 w-4"/>Voltar</Link><div className="mb-6 flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-primary/10 text-primary"><ScrollText className="h-5 w-5"/></div><div><h1 className="text-2xl font-bold">Logs do sistema</h1><p className="text-sm text-muted-foreground">Histórico das alterações realizadas pelos usuários.</p></div></div><div className="overflow-hidden rounded-xl border border-border bg-card"><table className="w-full text-left text-sm"><thead className="bg-muted/40 text-muted-foreground"><tr><th className="px-5 py-3">Usuário</th><th className="px-5 py-3">E-mail</th><th className="px-5 py-3">Alteração</th><th className="px-5 py-3">Data e hora</th></tr></thead><tbody>{loading?<tr><td colSpan={4} className="px-5 py-10 text-center text-muted-foreground">Carregando...</td></tr>:logs.length===0?<tr><td colSpan={4} className="px-5 py-10 text-center text-muted-foreground">Nenhuma alteração registrada.</td></tr>:logs.map(log=><tr key={log.id} className="border-t border-border"><td className="px-5 py-3 font-medium">@{log.user.username}</td><td className="px-5 py-3 text-muted-foreground">{log.user.email}</td><td className="px-5 py-3">{log.action}</td><td className="px-5 py-3 text-muted-foreground">{new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(log.createdAt))}</td></tr>)}</tbody></table></div></div></Layout> }
